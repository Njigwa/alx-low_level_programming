#include <stdio.h>
#include <ctype.h>
/**
 * main - A program that prints alphabetical letters in lowercase
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int x = 'A';

	for (x++;

	putchar(tolower(x));
	
	putchar('\n'));

	return (0);
}
