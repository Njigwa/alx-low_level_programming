#include <stdio.h>
#include <ctype.h>
/**
 * main - A program that prints alphabetical letters in lowercase
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int x = tolower(x);
	int lower_x = tolower(x);

	for (int x = 'A'; x <= 'Z'; x++)
		putchar(tolower(x));

		putchar('\n');

	return (0);
}
