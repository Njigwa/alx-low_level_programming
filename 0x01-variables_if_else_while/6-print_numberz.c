#include <stdio.h>
/**
 * main - A program that prints all single digit numbers of base ten
 *
 *Return: 0 (Succes)
 */
int main(void)
{
	int x;

	for (x = 0; x < 0; x++)
		putchar(x + '0');
	putchar('\n');
	return (0);
}
